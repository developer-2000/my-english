---
alwaysApply: true
---
ПРАВИЛА -
Всегда перед началом разработки читай этот файл и при завершении делай здесь редактирование того что сделано!
Когда чтото редактируешь здесь думай что пишешь!
Когда чтото говорю сделать не нужно делать сразу и то что не говорил !
И не пиши мне html примеры!
Твоя задача четко идя по плану проверки говорить что сделать и на какой странице после чего я показываю тебе вывод в консоле
и ты думая перепроверяешь если тебе чтото не ясно или завершаешь пункт проверки фиксируя результат в \.cursor\rules\looping-plan.mdc!
Все ли тебе ясно или еще чтото надо добавить в этом файле или в проверке?
Когда пишешь результат оставляй саму задачу и ниже пиши результат
✅ у положительного результата такая
🔄 у  задачи такая иконка
пример
это задача и ты обязан ее оставить как есть - 🔄 Найти все addEventListener в компонентах
А вот уже ответ на нее - и ты обязан вставить ответ под задачу !
✅ Найдены все addEventListener в компонентах:
PageListWords.vue: 2 addEventListener для select_type и update_select_type
ModalLearnWord.vue: 1 addEventListener для keydown (Escape)
Hallway.vue: 1 addEventListener для show.bs.modal
ПРОБЛЕМА: PageListWords.vue НЕ удаляет addEventListener в beforeDestroy
✅ Проверены removeEventListener в beforeDestroy:
✅ ModalLearnWord.vue: ЕСТЬ removeEventListener для keydown в closeModal
❌ PageListWords.vue: НЕТ removeEventListener для select_type и update_select_type
❌ Hallway.vue: НЕТ removeEventListener для show.bs.modal
⚠️ ПРОБЛЕМА: PageListWords.vue и Hallway.vue накапливают обработчики событий
---

План проверок на зацикливание в проекте
1. МИКСИНЫ - КРИТИЧЕСКИЕ МЕСТА
1.1 good_table_mixin.js
Потенциальные проблемы:

isRequestInProgress флаг может не сбрасываться
setTimeout в методах может создавать множественные таймеры
initialData() может вызываться рекурсивно
Проверки:

// В onSearch, onPageChange, onPerPageChange, onSortChange
console.log('🔍 [GOOD_TABLE] Method called:', methodName, 'isRequestInProgress:', this.isRequestInProgress);
console.log('🔍 [GOOD_TABLE] Server params:', JSON.stringify(this.serverParams));

// В updateParams
console.log('🔍 [GOOD_TABLE] Updating params:', JSON.stringify(newProps));

// В initialData (если есть)
console.log('🔍 [GOOD_TABLE] InitialData called, rows count:', this.table.rows.length);
1.2 help_search_word_mixin.js
Потенциальные проблемы:

searchWord() может вызываться при каждом вводе символа
searchHelpWord() может зациклиться при быстром вводе
Проверки:

// В searchWord
console.log('🔍 [HELP_SEARCH] searchWord called with:', word);
console.log('🔍 [HELP_SEARCH] isLoading:', this.isLoading);

// В searchHelpWord
console.log('🔍 [HELP_SEARCH] searchHelpWord called with:', word);
console.log('🔍 [HELP_SEARCH] help_dynamic length:', this.help_dynamic.length);
1.3 sound_word_mixin.js
Потенциальные проблемы:

forSpeak() может создавать множественные Promise.all
setTimeout в forSpeak может накапливаться
Проверки:

// В forSpeak
console.log('🔍 [SOUND] forSpeak called, arrText length:', this.speak.arrText.length);
console.log('🔍 [SOUND] speak.start:', this.speak.start);

// В readSound
console.log('🔍 [SOUND] readSound called for:', text);
2. VUE КОМПОНЕНТЫ
2.1 PageIndex.vue
Потенциальные проблемы:

Главная страница может не отображаться из-за ошибок инициализации
Роутер может не загружать компонент
Проверки:

// В mounted
console.log('🔍 [PAGE_INDEX] Component mounted');
console.log('🔍 [PAGE_INDEX] Links count:', this.links.length);

// В created (если есть)
console.log('🔍 [PAGE_INDEX] Component created');

2.2 PageListWords.vue
Потенциальные проблемы:

loadWordsAndTypes() может вызываться рекурсивно
updateColumnTable() с setTimeout может накапливаться
hoverWordShowTitle() может создавать множественные обработчики
Проверки:

// В loadWordsAndTypes
console.log('🔍 [PAGE_LIST_WORDS] loadWordsAndTypes called');
console.log('🔍 [PAGE_LIST_WORDS] URL:', url);
console.log('🔍 [PAGE_LIST_WORDS] Response data:', response.data);

// В updateColumnTable
console.log('🔍 [PAGE_LIST_WORDS] updateColumnTable called');
console.log('🔍 [PAGE_LIST_WORDS] Timer ID:', timerId);

// В hoverWordShowTitle
console.log('🔍 [PAGE_LIST_WORDS] hoverWordShowTitle called');
2.3 PageWordSentences.vue
Потенциальные проблемы:

loadSentences() может вызываться рекурсивно
activationButtonSoundInMenu() с setTimeout может накапливаться
Проверки:

// В loadSentences
console.log('🔍 [PAGE_WORD_SENTENCES] loadSentences called');
console.log('🔍 [PAGE_WORD_SENTENCES] URL:', url);

// В activationButtonSoundInMenu
console.log('🔍 [PAGE_WORD_SENTENCES] activationButtonSoundInMenu called');
3. WATCHERS И COMPUTED
3.1 Проверка watchers
Потенциальные проблемы:

Watchers могут вызывать методы, которые изменяют наблюдаемые свойства
Проверки:

// В каждом watcher
console.log('🔍 [WATCHER] Watcher triggered for:', propertyName);
console.log('🔍 [WATCHER] Old value:', oldValue);
console.log('🔍 [WATCHER] New value:', newValue);
3.2 Проверка computed
Потенциальные проблемы:

Computed свойства могут зависеть друг от друга
Проверки:

// В каждом computed
console.log('🔍 [COMPUTED] Computed property calculated:', propertyName);
4. HTTP ЗАПРОСЫ
4.1 Проверка дублирующихся запросов
Потенциальные проблемы:

Одинаковые запросы могут отправляться одновременно
Запросы могут не отменяться при размонтировании компонента
Проверки:

// Перед каждым HTTP запросом
console.log('🔍 [HTTP] Request started:', method, url);
console.log('🔍 [HTTP] Request data:', data);

// После каждого HTTP запроса
console.log('🔍 [HTTP] Request completed:', method, url);
console.log('🔍 [HTTP] Response status:', response.status);
5. TIMEOUT И INTERVAL
5.1 Проверка накопившихся таймеров
Потенциальные проблемы:

setTimeout/setInterval могут накапливаться
Таймеры могут не очищаться при размонтировании
Проверки:

// При создании таймера
const timerId = setTimeout(() => {
    console.log('🔍 [TIMER] Timer executed:', timerId);
}, delay);
console.log('🔍 [TIMER] Timer created:', timerId);

// При очистке таймера
clearTimeout(timerId);
console.log('🔍 [TIMER] Timer cleared:', timerId);
6. EVENT LISTENERS
6.1 Проверка множественных обработчиков
Потенциальные проблемы:

Обработчики событий могут добавляться многократно
Обработчики могут не удаляться при размонтировании
Проверки:

// При добавлении обработчика
console.log('🔍 [EVENT] Event listener added for:', eventType);

// При удалении обработчика
console.log('🔍 [EVENT] Event listener removed for:', eventType);
7. VUE LIFECYCLE
7.1 Проверка жизненного цикла
Потенциальные проблемы:

Методы могут вызываться после размонтирования компонента
Данные могут обновляться в несуществующих компонентах
Проверки:

// В mounted
console.log('🔍 [LIFECYCLE] Component mounted:', this.$options.name);

// В beforeDestroy
console.log('🔍 [LIFECYCLE] Component destroying:', this.$options.name);

// В методах после проверки существования компонента
if (this.$el) {
    console.log('🔍 [LIFECYCLE] Method called on existing component');
} else {
    console.log('🔍 [LIFECYCLE] Method called on destroyed component');
}
8. ПЛАН ВНЕДРЕНИЯ (СТАТУС ВЫПОЛНЕНИЯ)
8.1 Этап 1: Критические миксины ✅ ЗАВЕРШЕН
✅ Добавлено логирование в good_table_mixin.js - реализовано в методах onSearch, onPageChange, onPerPageChange, onSortChange, updateParams
✅ Добавлено логирование в help_search_word_mixin.js - реализовано в методах searchWord, searchHelpWord с дебаунсингом
✅ Добавлено логирование в sound_word_mixin.js - реализовано в методах forSpeak, readSound, playAudio

8.2 Этап 2: Основные компоненты ✅ ЗАВЕРШЕН
✅ Добавлено логирование в PageIndex.vue - реализовано в mounted, created
✅ Добавлено логирование в PageListWords.vue - реализовано в методах loadWordsAndTypes, updateColumnTable, hoverWordShowTitle, created/mounted
✅ Добавлено логирование в PageWordSentences.vue - реализовано в методах loadSentences, activationButtonSoundInMenu, bindCheckboxSound, createSentence, updateSentence, initialData, created/mounted

8.3 Этап 3: HTTP запросы ✅ ЗАВЕРШЕН
✅ Добавлено логирование HTTP запросов в http_client.js - интерцепторы для request/response
✅ Интерцепторы добавлены для всех axios запросов

8.4 Этап 4: Таймеры и события ✅ ЗАВЕРШЕН
✅ Добавлено логирование setTimeout в компонентах
✅ Добавлена очистка таймеров в beforeDestroy
✅ Добавлено логирование в sound_word_mixin.js - forSpeak, readSound

8.5 Этап 5: Защитные механизмы ✅ ЗАВЕРШЕН
✅ Добавлен дебаунсинг в help_search_word_mixin.js
✅ Добавлена защита от множественных запросов в good_table_mixin.js
✅ Добавлена очистка ресурсов в beforeDestroy всех компонентов
✅ Добавлены интерцепторы для всех HTTP запросов
9. КРИТЕРИИ ЗАЦИКЛИВАНИЯ
9.1 Признаки зацикливания:
Одинаковые сообщения в консоли повторяются более 3 раз подряд
HTTP запросы отправляются чаще чем раз в секунду
Таймеры создаются быстрее чем очищаются
Методы вызываются в бесконечном цикле
9.2 Действия при обнаружении:
Остановить выполнение
Проанализировать стек вызовов
Найти причину зацикливания
Добавить защитные механизмы
Протестировать исправление
10. ЗАЩИТНЫЕ МЕХАНИЗМЫ
10.1 Дебаунсинг для поиска
// Добавить в help_search_word_mixin.js
debounceSearch: debounce(function(word) {
    this.searchWord(word);
}, 300)
10.2 Ограничение частоты запросов
// Добавить в good_table_mixin.js
throttleRequest: throttle(function(method) {
    this[method]();
}, 1000)
10.3 Очистка таймеров
// В beforeDestroy каждого компонента
beforeDestroy() {
    if (this.timers) {
        this.timers.forEach(timerId => {
            clearTimeout(timerId);
            clearInterval(timerId);
        });
    }
}

11. СЛЕДУЮЩИЕ ШАГИ ПРОВЕРКИ

PageWordSentences.vue

🔄 Кнопка "Создать предложение" -
✅ НЕТ ЗАЦИКЛИВАНИЯ:
openModalCreateSentence 1 раз,
setVariableDefault 1 раз,
модальное окно открывается корректно
🔄 Ввод текста в модальном окне -
✅
searchHelpWord вызывается для каждого символа,
HTTP запросы работают корректно - статус 200
⚠️Ввод текста в модальном окне - ПРОБЛЕМА:
При вводе каждого символа отправляется HTTP запрос,
для "в" - запрос,
для "вв" - еще один запрос,
это может создавать много запросов при быстром вводе
❌ Нажатие Shift - КРИТИЧЕСКАЯ ПРОБЛЕМА:
ShiftLeft нажимается 30+ раз подряд,
создает спам в консоли,
после Shift отправляется 3 одинаковых HTTP запросов для "вв"
✅ Ввод "Hello" - ХОРОШЕЕ:
Поиск работает корректно,
help_dynamic обновляется правильно (285->75->10->0),
HTTP запросы завершаются успешно
⚠️ Ввод "Hello" - ПРОБЛЕМА:
При вводе каждого символа отправляется HTTP запрос (h->he->hel->hell),
создает много запросов
❌ Создание предложения - КРИТИЧЕСКАЯ ОШИБКА:
$(...).modal is not a function,
jQuery/Bootstrap не загружен,
HTTP запрос не отправляется
✅ Кнопка "Learn" - НЕТ ЗАЦИКЛИВАНИЯ:
openLearnModal 1 раз,
HTTP запрос 1 раз,
статус 200
⚠️ Кнопка "Learn" - ПРОБЛЕМА:
Кнопка пропадает после открытия модального окна и не появляется после закрытия
✅ Кнопка "Не знаю" - НЕТ ЗАЦИКЛИВАНИЯ:
HTTP запрос 1 раз,
action: "down",
sentence_id: 414,
статус 200
✅ Кнопка "Знаю" - НЕТ ЗАЦИКЛИВАНИЯ:
HTTP запрос 1 раз,
action: "up",
sentence_id: 415,
статус 200
✅ Кнопка переключения языка (Ru ~ Eng) - НЕТ ЗАЦИКЛИВАНИЯ:
switchLanguage вызывается корректно,
languageIndex переключается (0->1->0),
switchViewWord работает
✅ Кнопки редактирования в таблице - НЕТ ЗАЦИКЛИВАНИЯ:
btn_sentence clicked 1 раз,
getSentenceCollection 1 раз,
setVariableDefault 1 раз,
модальное окно открывается корректно
✅ Закрытие модального окна редактирования - НЕТ ЗАЦИКЛИВАНИЯ:
closeUpdateSentenceModal 1 раз,
модальное окно закрывается корректно
✅ Чекбокс озвучки - НЕТ ЗАЦИКЛИВАНИЯ:
HTTP запрос 1 раз,
status: true,
disabled_play обновляется корректно
⚠️ Чекбокс озвучки - ПРОБЛЕМА:
После изменения вызывается initialData() и перезагружается вся таблица,
создаются новые таймеры
✅ Поиск в таблице - НЕТ ЗАЦИКЛИВАНИЯ:
onSearch 1 раз,
isRequestInProgress работает корректно,
HTTP запрос 1 раз,
результаты обновляются
❌ Кнопка очистки поиска - КРИТИЧЕСКАЯ ПРОБЛЕМА:
initialData вызывается 3 раза,
loadSentences 3 раза,
HTTP запросы дублируются,
таймеры накапливаются
✅ Пагинация - НЕТ ЗАЦИКЛИВАНИЯ:
onPageChange 1 раз,
isRequestInProgress работает корректно,
HTTP запрос 1 раз,
страница 2 загружается
✅ Кнопка звукового перевода - НЕТ ЗАЦИКЛИВАНИЯ:
forSpeak 1 раз,
readSound для 2 предложений,
speak.start корректно
✅ Чекбокс повтора звука - НЕТ ЗАЦИКЛИВАНИЯ:
handleRepeatCheckboxChange вызывается корректно,
repeat_bool переключается (false->true->false),
count_repeat остается 1
✅ Поле количества повторений - НЕТ ЗАЦИКЛИВАНИЯ:
handleRepeatCountChange вызывается корректно,
count_repeat обновляется (1->3)
✅ Кнопка звукового перевода с повтором - НЕТ ЗАЦИКЛИВАНИЯ:
forSpeak 1 раз,
readSound для 2 предложений,
speak.start корректно,
repeat_bool: true,
count_repeat: 3
✅ Кнопки управления звуком - НЕТ ЗАЦИКЛИВАНИЯ:
pauseReadSound,
continueReadSound,
stopReadSound работают корректно,
состояния изменяются правильно

✅ PageListWords.vue ПРОВЕРЕН - НЕТ ЗАЦИКЛИВАНИЯ:
Component created 1 раз,
Component mounted 1 раз,
loadWordsAndTypes 1 раз,
HTTP запрос 1 раз,
таймеры работают корректно
⚠️ PageListWords.vue - ПРОБЛЕМА:
getRowForWord вызывается для каждого слова (10 раз),
это может быть избыточно при загрузке

✅ Поиск в PageListWords.vue - НЕТ ЗАЦИКЛИВАНИЯ:
onSearch 1 раз,
isRequestInProgress работает корректно,
HTTP запрос 1 раз,
результаты обновляются
⚠️ Поиск в PageListWords.vue - ПРОБЛЕМА:
getRowForWord вызывается для каждого слова (10 раз),
это избыточно при поиске

✅ Пагинация в PageListWords.vue - НЕТ ЗАЦИКЛИВАНИЯ:
onPageChange 1 раз,
isRequestInProgress работает корректно,
HTTP запрос 1 раз,
страница 2 загружается
⚠️ Пагинация в PageListWords.vue - ПРОБЛЕМА:
getRowForWord вызывается для каждого слова (10 раз),
это избыточно при пагинации

✅ Кнопки редактирования в PageListWords.vue - НЕТ ЗАЦИКЛИВАНИЯ:
Edit button clicked 1 раз,
openUpdateWordModal 1 раз,
searchSentences 1 раз,
HTTP запрос 1 раз,
найдено 2 предложения
❌ Кнопки редактирования в PageListWords.vue - КРИТИЧЕСКАЯ ОШИБКА:
После редактирования слова в модальном окне остается старое значение в поле поиска,
появляется ошибка "Поле не заполнено" для обязательных полей,
модальное окно не очищается после закрытия,
у модального окна нет скроллинга,
footer модалки перекрывает body собой
⚠️ Ввод текста в модальном окне редактирования слова - ПРОБЛЕМА:
HTTP запрос для каждого символа,
capitalizeFirstLetter вызывается многократно для одних предложений

✅ Создание слова - ОТКРЫТИЕ МОДАЛЬНОГО ОКНА - НЕТ ЗАЦИКЛИВАНИЯ:
openModalCreateWord 1 раз,
setVariableDefault 1 раз,
setStyleDataModal 1 раз,
модальное окно открывается корректно,
переменные инициализируются правильно

✅ Ввод текста в модальном окне создания слова - ХОРОШЕЕ:
searchHelpWord вызывается для каждого символа,
HTTP запросы работают корректно,
статус 200,
help_dynamic обновляется правильно (8->485->43)
⚠️ Ввод текста в модальном окне создания слова - ПРОБЛЕМА:
При вводе каждого символа отправляется HTTP запрос,
для "t" - запрос,
для "te" - еще один запрос,
это может создавать много запросов при быстром вводе

❌ Select типа слова в модальном окне создания - КРИТИЧЕСКОЕ ЗАЦИКЛИВАНИЕ:
Один клик в select вызвал 6 одинаковых событий,
select_type changed 6 раз,
setStyleDataModal 6 раз,
Style data set 6 раз,
это классическое зацикливание обработчика события

✅ Чекбокс "Сгенерировать" в модальном окне создания слова - НЕТ ЗАЦИКЛИВАНИЯ:
toggleSwitch вызывается корректно,
состояние переключается (true->false),
таймеры работают правильно,
label обновляется корректно

✅ Создание слова - НЕТ ЗАЦИКЛИВАНИЯ:
createWord 1 раз,
HTTP запрос 1 раз (POST /word),
статус 200,
модальное окно закрывается корректно,
таблица перезагружается
⚠️ Создание слова - ПРОБЛЕМЫ:
getRowForWord вызывается для каждого слова (10 раз),
таймеры накапливаются,
обработчики событий добавляются повторно

⚠️ Кнопка удаления слова - ДУБЛИРОВАНИЕ:
Один клик вызвал 2 одинаковых события,
Delete button clicked 2 раза,
Word to delete 2 раза,
getRowForWord 2 раза,
confirmMessage 2 раза,
это указывает на дублирование обработчика события
✅ Удаление слова - НЕТ ЗАЦИКЛИВАНИЯ:
deleteWord 1 раз,
HTTP запрос 1 раз (POST /word/delete-word),
статус 200,
таблица перезагружается корректно,
страница переключается на 1

❌ Select типа слова в таблице - КРИТИЧЕСКОЕ ДУБЛИРОВАНИЕ:
Один выбор в select вызвал 2 одинаковых HTTP запроса,
initialData called 2 раза,
loadWordsAndTypes called 2 раза,
Making HTTP request 2 раза,
HTTP Request started 2 раза с одинаковыми параметрами,
это указывает на дублирование вызовов при изменении типа слова

✅ Кнопка "Изучить слово" - НЕТ ЗАЦИКЛИВАНИЯ:
openLearnModal 1 раз,
HTTP запрос 1 раз (POST /word/learn/get-word),
статус 200,
таблица перезагружается корректно
⚠️ Кнопка "Изучить слово" - ПРОБЛЕМЫ:
getRowForWord вызывается для каждого слова (10 раз),
таймеры накапливаются,
обработчики событий добавляются повторно

✅ Переключатель языка в модальном окне изучения слова - НЕТ ЗАЦИКЛИВАНИЯ:
switchLanguage вызывается корректно,
languageIndex переключается (0->1->0),
switchViewWord работает,
viewLanguageLine обновляется правильно (Eng ~ Ru ↔ Ru ~ Eng)

✅ Кнопка "Не знаю" в модальном окне изучения слова - НЕТ ЗАЦИКЛИВАНИЯ:
HTTP запрос 1 раз (POST /word/learn/get-word),
action: "up",
статус 200,
таблица перезагружается корректно
⚠️ Кнопка "Не знаю" - ПРОБЛЕМЫ:
getRowForWord вызывается для каждого слова (10 раз),
таймеры накапливаются,
обработчики событий добавляются повторно

❌ Кнопка "Знаю" в модальном окне изучения слова - КРИТИЧЕСКАЯ ОШИБКА:
HTTP запрос отправляется корректно,
action: "down",
но сервер возвращает 404 Not Found,
ошибка в checkSuccess - response.data is undefined

✅ Ввод текста в модальном окне изучения слова - ХОРОШЕЕ:
searchHelpWord вызывается для каждого символа,
HTTP запросы работают корректно,
статус 200,
help_dynamic обновляется правильно (0->284->74)
⚠️ Ввод текста в модальном окне изучения слова - ПРОБЛЕМА:
При вводе каждого символа отправляется HTTP запрос,
для "l" - запрос,
для "le" - еще один запрос,
viewLanguageLine вызывается избыточно при каждом вводе

✅ Ввод правильного слова в модальном окне изучения - НЕТ ЗАЦИКЛИВАНИЯ:
searchHelpWord вызывается корректно,
HTTP запрос 1 раз,
статус 200,
help_dynamic обновляется правильно (4->4)

✅ Пагинация - изменение количества строк - НЕТ ЗАЦИКЛИВАНИЯ:
onPerPageChange 1 раз,
HTTP запрос 1 раз,
perPage изменяется (10->50),
страница сбрасывается на 1,
данные загружаются корректно (50 записей)
⚠️ Пагинация - изменение количества строк - ПРОБЛЕМЫ:
getRowForWord вызывается для каждого слова (50 раз),
JavaScript отрабатывает 50 одинаковых операций,
это создает избыточную нагрузку на производительность,
таймеры накапливаются,
обработчики событий добавляются повторно

СЛЕДУЮЩИЙ ШАГ: Протестировать другие функции на странице /page-list-words или перейти к проверке других страниц

11.2 Проверка дублирующихся запросов:
✅ В PageListWords.vue - ЗАЩИТА ОТ ПАРАЛЛЕЛЬНЫХ ЗАПРОСОВ РАБОТАЕТ:
Быстро ввести символы в поле поиска (запрос отправляется автоматически),
при вводе "m" - запрос отправляется,
при вводе "mo" - isRequestInProgress: true и Request in progress, skipping...,
второй запрос блокируется, пока первый не завершится,
нет дублирования HTTP запросов

🔄 В PageWordSentences.vue - проверить что loadSentences не дублируется
✅ В PageWordSentences.vue - ЗАЩИТА ОТ ПАРАЛЛЕЛЬНЫХ ЗАПРОСОВ РАБОТАЕТ:
Быстро ввести символы в поле поиска (запрос отправляется автоматически),
при вводе "w" - запрос отправляется,
при вводе "wr" - isRequestInProgress: true и Request in progress, skipping...,
второй запрос блокируется, пока первый не завершится,
нет дублирования HTTP запросов

🔄 В миксинах - проверить что HTTP запросы отменяются при новых вызовах
✅ В миксинах - ЗАЩИТА ОТ ПАРАЛЛЕЛЬНЫХ ЗАПРОСОВ РАБОТАЕТ:
Быстро ввести символы в поле поиска (запрос отправляется автоматически),
при вводе "w" - запрос отправляется и находит 272 записи,
при вводе "wr", "wri" - Request in progress, skipping...,
при вводе "writ" - новый запрос отправляется после завершения предыдущего,
поиск работает корректно после исправления бэкенда

🔄 Найти все addEventListener в компонентах
11.3 Проверка event listeners:
✅ Найдены все addEventListener в компонентах:
PageListWords.vue: 2 addEventListener для select_type и update_select_type
ModalLearnWord.vue: 1 addEventListener для keydown (Escape)
Hallway.vue: 1 addEventListener для show.bs.modal
ПРОБЛЕМА: PageListWords.vue НЕ удаляет addEventListener в beforeDestroy
✅ Проверены removeEventListener в beforeDestroy:
✅ ModalLearnWord.vue: ЕСТЬ removeEventListener для keydown в closeModal
❌ PageListWords.vue: НЕТ removeEventListener для select_type и update_select_type
❌ Hallway.vue: НЕТ removeEventListener для show.bs.modal
⚠️ ПРОБЛЕМА: PageListWords.vue и Hallway.vue накапливают обработчики событий

🔄 Проверить window/document event listeners:
   - Найти обработчики на window и document
   - Убедиться что они удаляются при размонтировании
✅ Проверены window/document event listeners:
✅ ModalLearnWord.vue: document.addEventListener для keydown ЕСТЬ и удаляется в closeModal
✅ НЕТ window.addEventListener в компонентах
✅ Все document event listeners правильно удаляются

11.4 Мониторинг производительности:
🔄 Использовать Performance tab в DevTools
   - Записать профиль при загрузке страницы
   - Проверить на утечки памяти
✅ Анализ кода на потенциальные проблемы производительности:
❌ PageListWords.vue: 3 setTimeout без clearTimeout в beforeDestroy
❌ PageWordSentences.vue: 2 setTimeout без clearTimeout в beforeDestroy
❌ ModalLearnWord.vue: 1 setTimeout без clearTimeout в beforeDestroy
⚠️ ПРОБЛЕМА: Таймеры накапливаются и не очищаются при размонтировании компонентов

🔄 ДОРАБОТКА - ПРОБЛЕМА С АВТОРИЗАЦИЕЙ:
❌ При открытии http://english.my/page-list-words без авторизации выдает ошибку:
ErrorException: Header may not contain more than a single header, new line detected
ПРОБЛЕМА: Нет редиректа на страницу login если не авторизован

🔄 Проверить Memory tab на утечки памяти
   - Сделать снимок памяти
   - Выполнить действия на странице
   - Сделать второй снимок
   - Сравнить утечки
✅ Performance профиль проанализирован:
INP: 61ms (отлично), CLS: 0.00 (отлично)
Scripting: 1,124ms, System: 807ms, Rendering: 616ms
⚠️ [unattributed]: 2,352.8ms - много времени на неизвестные операции
⚠️ Extensions: 325ms - влияют на производительность
🔄 Мониторить Network tab на лишние запросы
   - Проверить дублирующиеся HTTP запросы
   - Проверить отмененные запросы

12. ДОПОЛНИТЕЛЬНЫЕ ФАЙЛЫ ДЛЯ ПРОВЕРКИ
12.1 Компоненты требующие внимания:
🔄 HelpSearchWord.vue - может содержать дополнительные поисковые функции
   - Проверить поисковые функции на зацикливание
   - Проверить обработчики событий
✅ HelpSearchWord.vue проанализирован:
Компонент простой - только отображает helpDynamic через v-html
Используется в ModalLearnWord.vue, PageWordSentences.vue, PageListWords.vue
НЕТ зацикливания - только watcher для helpDynamic без логики
НЕТ обработчиков событий - только отображение данных

🔄 ModalLearnWord.vue - модальные окна могут накапливать обработчики
   - ✅ Добавлено логирование в openLearnModal, closeModal, handleEscapeKey
   - ✅ Добавлена защита от накопления обработчиков событий
   - ✅ Добавлена очистка в beforeDestroy
   - ✅ Добавлены счетчики eventListenersCount
   - ✅ ТЕСТИРОВАНИЕ ЗАВЕРШЕНО: открыл/закрыл модальное окно 10 раз и проверил счетчики
   - ✅ Escape listeners: 0 (не накапливаются)
   - ✅ Mouseover listeners: 0 (не накапливаются)
   - ✅ Все обработчики событий правильно очищаются
   - ✅ Нет утечек памяти
   - ✅ Защита от накопления обработчиков работает корректно


