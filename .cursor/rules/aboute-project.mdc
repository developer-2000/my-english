---
alwaysApply: true
---
Всегда перед началом разработки читай этот файл!

# My English - Правила разработки проекта

## 🏗️ Архитектура проекта

### Backend (Laravel 8.54)
- **Аутентификация**: Laravel Sanctum + система ролей (admin/user)
- **База данных**: MySQL с поддержкой английского и румынского языков
- **API**: RESTful API с валидацией через Form Requests
- **Очереди**: Laravel Queues для email верификации
- **Бэкапы**: Spatie Laravel Backup с middleware контролем

### Frontend (Vue.js 2.6.12 + Vite)
- **SPA**: Vue Router для навигации
- **Состояние**: Vuex для управления данными
- **UI**: Bootstrap 5.3.3 + Vue компоненты
- **Интерактивность**: Tippy (тултипы), SweetAlert2 (модалки), Vue Good Table
- **Сборщик**: Vite (заменил webpack) - быстрая разработка с HMR

## 📁 Ключевые директории и файлы

### Backend структура
```
app/
├── Http/
│   ├── Controllers/           # Основные контроллеры
│   │   ├── AuthController.php           # Аутентификация
│   │   ├── WordController.php           # Управление словами
│   │   ├── SentenceController.php       # Управление предложениями
│   │   ├── LanguageController.php       # Языковые настройки
│   │   └── GeneratingSentencesAiController.php  # ИИ генерация
│   ├── Middleware/
│   │   ├── CheckRoleMiddleware.php      # Проверка ролей
│   │   └── BackupDatabase.php           # Контроль бэкапов
│   ├── Requests/              # Валидация данных
│   └── Responses/
│       └── ApiResponse.php    # Стандартизированные ответы
├── Models/                    # Eloquent модели
│   ├── User.php              # Пользователи + роли
│   ├── EnWord.php / RoWord.php    # Слова EN/RO
│   ├── EnSentence.php / RoSentence.php  # Предложения EN/RO
│   ├── Language.php          # Поддерживаемые языки
│   └── LanguageUser.php      # Связь пользователь-язык
├── Repositories/             # Слой доступа к данным
└── Services/                 # Бизнес-логика
    └── Translations/         # Сервисы перевода
```

### Frontend структура
```
resources/js/
├── components/
│   ├── Hallway.vue           # Главный layout компонент
│   ├── Pages/                # Страницы приложения
│   └── details/              # Детальные компоненты
├── router/
│   └── index.js             # Vue Router конфигурация
├── store/
│   └── index.js             # Vuex store
├── mixins/                  # Общая логика компонентов
├── services/
│   └── http_client.js       # HTTP клиент для API
└── app.js                   # Точка входа Vue приложения

# Конфигурация сборки
vite.config.js               # Конфигурация Vite
package.json                  # Зависимости (type: "module")
```

## 🔑 Основные модели и их отношения

### User (Пользователь)
- **Роли**: belongsToMany(Role) - admin/user
- **Языки**: hasOne(LanguageUser) - интерфейс и изучаемый язык
- **Автоматически**: назначаются языки по умолчанию (ru/en)

### EnWord / RoWord (Слова)
- **Тип**: belongsTo(EnWordType/RoWordType)
- **Временные формы**: JSON поле для глаголов
- **Методы**: processWords() - обработка массива слов

### EnSentence / RoSentence (Предложения)
- **Озвучка**: hasOne(EnSentenceSound/RoSentenceSound)
- **Перевод**: автоматический через Google Translate
- **Приоритет**: для системы изучения

## 🌐 API Endpoints

### Аутентификация
- `POST /auth/login` - вход
- `POST /auth/register` - регистрация с email верификацией
- `GET /auth/logout` - выход

### Слова (WordController)
- `GET /word` - список слов с пагинацией
- `POST /word` - добавление слова
- `POST /word/update-word` - обновление
- `POST /word/delete-word` - удаление
- `POST /word/learn/get-word` - получение для изучения

### Предложения (SentenceController)
- `GET /sentence` - список предложений
- `POST /sentence` - добавление предложения
- `POST /sentence/search-sentences` - поиск
- `POST /sentence/learn/get-sentence` - для изучения
- `POST /sentence/bind-checkbox-sound` - привязка озвучки

### ИИ генерация (GeneratingSentencesAiController)
- `POST /ai/generate-sentences` - генерация через AI21 Studio API

## 🎯 Ключевые функции

### Система изучения
1. **Слова**: приоритизация, временные формы, переводы
2. **Предложения**: автоматический перевод, озвучка, поиск
3. **ИИ генерация**: создание предложений на основе слов
4. **Прогресс**: отслеживание изучения

### Многоязычность
- **Интерфейс**: русский/английский
- **Изучение**: английский/румынский
- **Автоперевод**: Google Translate PHP
- **i18n**: Vue-i18n для интерфейса

### Безопасность
- **CSRF**: защита всех форм
- **Валидация**: Form Requests для всех endpoints
- **Роли**: middleware проверка ролей
- **Rate limiting**: ограничение запросов

## 🔧 Конфигурация

### .env переменные
```env
# База данных
DB_CONNECTION=mysql
DB_DATABASE=my_english

# Email (Mailtrap для разработки)
MAIL_USERNAME=your_mailtrap_username
MAIL_PASSWORD=your_mailtrap_password

# ИИ API
STUDIO_AI_API_KEY=your_ai21_api_key

# Приложение
APP_URL=http://your-domain
```

### Учетные данные по умолчанию
- **Admin**: admin@admin.admin / admin
- **Роли**: автоматически назначается 'user'

## 🚀 Команды разработки

### Laravel команды
```bash
# Очистка кэша
php artisan route:clear && php artisan view:clear && php artisan config:clear

# Технический роут
GET /technical/artisan/clear_all

# Очереди (для email)
php artisan queue:work

# Миграции
php artisan migrate --seed
```

### Frontend команды (Vite)
```bash
# Разработка с HMR
npm run dev

# Продакшен сборка
npm run build

# Отслеживание изменений
npm run watch

# Установка зависимостей
npm install
```

## 📋 Паттерны разработки

### Backend
- **Repository Pattern**: слой доступа к данным
- **Service Pattern**: бизнес-логика в сервисах
- **Form Requests**: валидация входящих данных
- **API Resources**: стандартизированные ответы

### Frontend
- **Component Composition**: переиспользуемые компоненты
- **Mixin Pattern**: общая логика между компонентами
- **Vuex Store**: централизованное управление состоянием
- **HTTP Service**: абстракция API вызовов
- **ES Modules**: современные импорты для Vite

## 🎨 UI/UX особенности

### Компоненты
- **Vue Good Table**: таблицы с сортировкой/фильтрацией
- **Vue Tippy**: информативные тултипы
- **SweetAlert2**: красивые модальные окна
- **Bootstrap Toggle**: переключатели

### Интерактивность
- **Озвучка**: Web Speech API для произношения
- **Поиск**: реальное время поиска по словам/предложениям
- **Drag & Drop**: для управления порядком
- **Responsive**: адаптивный дизайн

## 🔍 Поиск и навигация

### Где искать код:
- **Аутентификация**: `AuthController.php`, `User.php`, `CheckRoleMiddleware.php`
- **Слова**: `WordController.php`, `EnWord.php`, `WordRepository.php`
- **Предложения**: `SentenceController.php`, `EnSentence.php`, `SentenceRepository.php`
- **ИИ**: `GeneratingSentencesAiController.php`
- **Frontend**: `Hallway.vue`, `http_client.js`, Vuex store
- **Сборка**: `vite.config.js`, `package.json`

### Ключевые миксины:
- `response_methods_mixin.js` - обработка API ответов
- `translation_i18n_mixin.js` - переводы
- `user_mixin.js` - работа с пользователем
- `good_table_mixin.js` - таблицы

## ⚠️ Важные моменты

1. **Языки**: проект поддерживает EN/RO слова и предложения
2. **Роли**: обязательная проверка ролей через middleware
3. **Бэкапы**: автоматические при посещении определенных страниц
4. **Email**: требует настройки Mailtrap для верификации
5. **ИИ API**: требует API ключ AI21 Studio
6. **Очереди**: обязательный запуск `php artisan queue:work`
7. **Vite**: используйте `npm run dev` для разработки с HMR

## 🔄 Миграция на Vite (2024)

### Что изменено:
- **Сборщик**: webpack → Vite
- **Конфигурация**: `webpack.mix.js` → `vite.config.js`
- **Импорты**: require() → ES modules (import/export)
- **Blade шаблоны**: `{{ mix() }}` → условная загрузка для dev/prod
- **Package.json**: добавлен `"type": "module"`

### Преимущества:
- **Быстрая сборка**: esbuild для мгновенной компиляции
- **HMR**: Hot Module Replacement без перезагрузки страницы
- **ES модули**: современный подход к импортам
- **Оптимизация**: лучшая оптимизация для продакшена

### Файлы миграции:
- `VITE_MIGRATION.md` - подробная документация
- `vite.config.js` - конфигурация Vite
- Обновленные Blade шаблоны с условной загрузкой

## 🐛 Отладка

### Логи
- `storage/logs/laravel.log` - Laravel логи
- `storage/logs/backup.log` - логи бэкапов

### Тестирование
- `tests/Feature/` - функциональные тесты
- `tests/Unit/` - модульные тесты

### Профилирование
- Laravel Telescope (если установлен)
- Vue DevTools для frontend отладки (отключены предупреждения)

### Vite отладка
- Dev сервер: `http://localhost:5173/`
- HMR: автоматическое обновление при изменении файлов
- Консоль браузера: логи Vite и Vue приложения
